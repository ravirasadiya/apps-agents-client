import AgentDealsTable from "@/component/DealsAgents/AgentDealsTable";
import DateAndSelect, { Filters } from "@/component/dashboard/DateAndSelect";
import Layout from "@/component/layouts/Layout";
import { Box } from "@mui/material";
import Typography from "@mui/material/Typography";
import Head from "next/head";

export default function Dealsagents() {
  const onFilterChanged = (filters: Filters) => {
    console.log("on filter changed");
  };

  return (
    <Layout>
      <Head>
        <title>Agent Deals</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>
        <Box>
          <Typography className="def_had_txt">Deals (for agents)</Typography>
          <DateAndSelect onFilterChange={onFilterChanged} />
          <AgentDealsTable />
        </Box>
      </div>
    </Layout>
  );
}
